e<div>
  <%= simple_form_for @agenda do |f| %>
  	<%= f.input :nom %>
	<%= f.input :description %>
	<%= f.input :date %>
	<%= f.input :photo %>
	<%= f.input :photo_cache, as: :hidden %>
	<%= f.button :submit, "ajouter un evenement a l'agenda", class: "button" %>
  <% end %>
</div>
